{
  "name": "whetstone",
  "version": "0.3.0",
  "private": true,
  "description": "Agent-native spaced-retrieval understanding system over plain markdown. This package.json exists only to run the hub's stress-test harness — Whetstone itself has zero runtime dependencies.",
  "license": "MIT",
  "scripts": {
    "test": "node test/hub.test.mjs && node test/version.test.mjs"
  }
}
